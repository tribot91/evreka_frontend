import React from 'react';
import Input from '@material-ui/core/Input';

const Route = ({ name, email, id }) => {
  return (
    <div style={{marginRight: '10px'}}>
        <Input></Input>
    </div>
  );
}

export default Route;
